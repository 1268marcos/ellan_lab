{
  "title": "SLOs",
  "panels": [
    {
      "title": "Error rate SLO",
      "targets": [{ "expr": "inventory_stream_errors_total" }]
    },
    {
      "title": "Latency p95 (placeholder)",
      "targets": [{ "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))" }]
    }
  ],
  "schemaVersion": 38,
  "version": 1
}
