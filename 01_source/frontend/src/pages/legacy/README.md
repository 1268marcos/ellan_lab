# Páginas legadas (ELLAN LAB)

Ficheiros nesta pasta **não** devem ser importados por rotas ativas em `App.jsx`.

- **`LockerDashboardFirst.jsx`** — protótipo histórico da rota `/ops/00`. Mantido só para referência ou correções pontuais; o utilizador vê a mensagem de descontinuação em `/ops/00` via `OpsDiscontinuedEllanLabPage`.

- **`RegionPageFirst.jsx`** — kiosk legado da rota `/ops/00/kiosk` (modo SP). Mesmo tratamento: rota ativa mostra `OpsDiscontinuedEllanLabPage`; ficheiro isolado aqui.
