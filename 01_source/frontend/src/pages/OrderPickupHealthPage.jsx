import React from "react";
import PickupHealthPage from "./PickupHealthPage";

/** OPS order lifecycle — mesma UI que `/ops/analytics/pickup`, rota canônica `/ops/order/pickup-health`. */
export default function OrderPickupHealthPage() {
  return <PickupHealthPage />;
}
