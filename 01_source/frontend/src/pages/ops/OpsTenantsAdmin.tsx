export default function OpsTenantsAdmin() {
  return (
    <div className="mx-auto max-w-3xl p-6">
      <h1 className="text-xl font-semibold">OPS · Tenants</h1>
      <p className="mt-2 text-sm text-gray-500">
        Módulo tenants via <code>partner_admin_service</code> (porta 8016). Implementação completa pendente.
      </p>
    </div>
  )
}
