================================================================================
ELLAN LAB — Acompanhamento de Sprints · Fiscal & Invoices
================================================================================
Fonte de planejamento: docs/Sprint_Backlog_Fiscal_and_Invoices.pdf
Serviço principal: billing_fiscal_service
Última atualização do plano (PDF): 2026-04-23

Objetivo
--------
Evoluir o billing_fiscal_service do estado stub atual para padrão de indústria de
lockers: NFC-e modelo 65, cálculo de tributos, integrações SEFAZ (BR) e AT (PT),
cancelamento, reconciliação fiscal e integração ao domínio de Orders (contrato
enriquecido + stub coordenado).

Resumo quantitativo
-------------------
  3 sprints eixo Fiscal   (F-1, F-2, F-3)
  3 sprints eixo Invoice  (I-1, I-2, I-3)
  1 sprint Orders (stub)  (O-1)
  ~103 story points total (referência do backlog)

Sequenciamento oficial (dependências)
-------------------------------------
  O-1  →  desbloqueador da cadeia (contrato rico Orders ↔ Fiscal)

  Após O-1, em paralelo:
    F-1  (schema / documento unificado — pré-requisito dos demais fiscais)
    I-1  (integração Orders → invoice completa)

  Depois de F-1 e I-1, em paralelo:
    F-2  (tax engine BR + PT)
    I-2  (cancelamento + entrega DANFE)

  Encerramento produção:
    F-3  (provedores reais NFC-e / SAT / AT — exige F-2 para XML correto)
    I-3  (reconciliação, monitoramento, exports SPED / SAF-T)

Legenda de status (preencher ao longo dos sprints)
----------------------------------------------------
  [ ] Não iniciado   [~] Em andamento   [x] Concluído   [!] Bloqueado / risco

-------------------------------------------------------------------------------
SPRINT O-1 — Order-to-invoice contract & event enrichment (Orders stub)
-------------------------------------------------------------------------------
Duração referência: 1 semana  |  Esforço: 8 pts
Escopo: não refatorar o domínio Orders completo; enriquecer contrato interno
        para o billing_fiscal_service consumir dados fiscais completos.

  Contexto
  - Novo endpoint interno no order_pickup_service com payload que a invoice
    precisa (locker_address, tenant_cnpj, consumer_cpf).
  - Desbloqueia F-1 e I-1.

  User stories
  [x] API: GET /internal/orders/{id}/fiscal-context — order + items +
        locker_address + tenant_cnpj + consumer_cpf (contrato versionado v2)
  [x] EVENT: order.paid payload v2 — locker_id, tenant_id, consumer_cpf
        (nullable), region, currency
  [x] SCHEMA: tenant_fiscal_config — tenant_id, CNPJ, razão social, IE, regime,
        CRT, cert A1 (ref vault)
  [x] API: GET /internal/fiscal/health — SEFAZ/AT acessíveis (consulta locker)

  Registro de evolução (datas / notas / PR / commit)
  - 2026-04-23: O-1 implementado no código — order_pickup_service (endpoints,
    tabela tenant_fiscal_config, modelo Order.tenant_id, outbox v2) +
    billing_fiscal_service (get_order_snapshot_for_invoice → fiscal-context).
    Variável opcional BILLING_FISCAL_SERVICE_URL no pickup para health remoto.


-------------------------------------------------------------------------------
SPRINT F-1 — Schema evolution & document unification (Fiscal)
-------------------------------------------------------------------------------
Duração referência: 2 semanas  |  Esforço: 13 pts
Objetivo: unificar fiscal_documents e invoices; campos obrigatórios NFC-e 65;
          contexto locker no documento. Pré-requisito para os demais sprints
          fiscais.

  User stories
  [x] SCHEMA: fiscal_doc_subtype — valores VARCHAR (NFC_E_65, NFE_55, …) +
        default NFC_E_65 no billing_fiscal_service.invoices
  [x] SCHEMA: emission_mode — ONLINE (+ constantes CONTINGENCY_SVRS, OFFLINE_SAT)
  [x] SCHEMA: locker context na invoice — locker_id, totem_id, slot_label;
        locker_address JSONB
  [x] SCHEMA: emitter & consumer — emitter_*, consumer_*, items_json JSONB
  [x] REFACTOR: leituras passam a preferir invoice no billing_fiscal_service
        (BILLING_FISCAL_SERVICE_URL); fiscal_documents permanece para stub
        local / reimpressão até remoção futura.

  DDL resumo (invoices — referência backlog)
  - locker_id, totem_id, fiscal_doc_subtype, emission_mode
  - emitter_cnpj, emitter_name, consumer_cpf, consumer_name, locker_address
  - items_json (espelho order_items)

  Registro de evolução
  - 2026-04-23: F-1 no billing_fiscal_service — colunas na tabela invoices
    (migrations REQUIRED_COLUMNS + índices), modelo SQLAlchemy, preenchimento
    via invoice_snapshot_fiscal.fiscal_columns_from_order_snapshot no
    ensure_invoice_for_order e generate_invoice; API InvoiceResponse; stub
    SEFAZ-SP usa colunas F-1. Testes: tests/test_invoice_snapshot_fiscal.py
    (pytest na .venv do serviço).
  - 2026-04-23: Redirecionamento de leitura no order_pickup_service —
    integrations/billing_fiscal_client, fiscal_read_adapter.FiscalReadView,
    fiscal_resolve.resolve_fiscal_for_order; internal/status
    (fiscal_invoice_in_billing), public_orders, kiosk identify,
    get_fiscal_document_by_order; /public/fiscal/by-code permanece só
    fiscal_documents (comentário na rota). InvoiceResponse + issued_at etc.
    no billing. Testes: tests/test_fiscal_read_adapter.py.


-------------------------------------------------------------------------------
SPRINT F-2 — Tax calculation engine — BR + PT (Fiscal)
-------------------------------------------------------------------------------
Duração referência: 2 semanas  |  Esforço: 21 pts
Objetivo: implementar tax_service.py; ICMS por UF (CONFAZ), PIS/COFINS, NCM;
          Portugal IVA por categoria (CIVA); breakdown por order_item.

  User stories
  [x] SERVICE: TaxEngine BR — ICMS por UF, PIS 0,65%, COFINS 3%, CST 102/000,
        lookup NCM via product_fiscal_config (opcional)
  [x] SERVICE: TaxEngine PT — IVA 23/13/6/isenta por iva_category
  [x] SCHEMA: product_fiscal_config — tabela + modelo SQLAlchemy
  [x] SERVICE: tax_breakdown_json + integração emissão (apply_tax antes do
        route_issue; SEFAZ/AT stubs leem breakdown)
  [x] TEST: pytest — SP, RJ, PT, Simples (tests/test_tax_service.py)

  Registro de evolução
  - 2026-04-23: F-2 — tax_service.build_tax_breakdown / apply_tax_to_invoice,
    coluna invoices.tax_breakdown_json, product_fiscal_config, ICMS_UF_RATE,
    IVA_PT_RATE, regime Simples (ICMS zero + PIS/COFINS). CI: rodar pytest
    no billing_fiscal_service.


-------------------------------------------------------------------------------
SPRINT F-3 — Provider integration — NFC-e & SAT (BR) + AT (PT) (Fiscal)
-------------------------------------------------------------------------------
Duração referência: 3 semanas  |  Esforço: 34 pts
Objetivo: substituir stubs sefaz_sp_service / at_pt_provedor; contingência SAT;
          DANFE térmica; callbacks autoridade.

  User stories
  [~] INTEGRATION: NFC-e XML schema v4.0 (NT 2015.002 + 2019.001), assinatura A1
  [ ] INTEGRATION: client SVRS/SEFAZ — autorização, retry lote
  [x] INTEGRATION: modo contingência SAT/MFe offline + sync ao reconectar
  [ ] INTEGRATION: client AT Portugal real (REST AT2024 ou SOAP legacy)
  [x] INTEGRATION: POST /internal/fiscal/callback — retorno async autoridades
  [~] DELIVERY: DANFE PDF simplificado + payload JSON impressora térmica
        (MQTT/HTTP)

  [~] DELIVERY: fila `invoice_email_outbox` + 4º loop no `invoice_issue_worker`;
        env `INVOICE_SMTP_ENABLED`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`,
        `SMTP_FROM`, `SMTP_USE_TLS` / `SMTP_USE_SSL`; log `SENT` | `SENT_STUB` | retries
  [~] INTEGRATION: CC-e stub BR — campo `xml_event_preview` (evento 110110) + espelho em `raw`
        (pré-SVRS; não assinado)
  [~] INTEGRATION: feature flags de provider real BR/PT no router
        (`FISCAL_REAL_PROVIDER_BR_ENABLED`, `FISCAL_REAL_PROVIDER_PT_ENABLED`)
        com adapters `*_real_adapter` e fallback explícito para stubs atuais
  [~] INTEGRATION: client HTTP real BR/PT com timeout/retry + normalização para contrato
        interno (`issue/cancel/correction-event`) e fallback em indisponibilidade
  [~] OPS: painel mínimo de providers fiscais (status por país, health/last_error,
        botão de teste de conectividade BR/PT)

  SCHEMA: fiscal_authority_callbacks (invoice_id, authority, raw_payload, ...)

  Registro de evolução
  - 2026-04-23: F-3 parcial — tabela `invoice_email_outbox` (migração startup),
    `invoice_email_queue_service` (claim/retry/backoff), SMTP real quando habilitado;
    `sefaz_sp_cc_e_stub` passa a expor `xml_event_preview` orientado ao evento SEFAZ.
  - 2026-04-24: F-3 slice 1 — `fiscal_router_service` passa a selecionar provider por
    feature flag (BR/PT); novos adapters `sefaz_svrs_real_adapter` e
    `at_pt_real_adapter` adicionam ponto único para integração real, preservando
    fallback para os stubs.
  - 2026-04-24: F-3 slice 2 — `fiscal_real_provider_client` (requests + timeout/retry)
    conectado aos adapters BR/PT; respostas normalizadas para o contrato atual de
    emissão/cancelamento/correção; falha de provider aciona fallback automático.
  - 2026-04-24: F-3 ops — endpoints `GET /admin/fiscal/providers/status` e
    `POST /admin/fiscal/providers/test-connectivity`; tabela
    `fiscal_provider_health_status`; página `ops /fiscal/providers` no frontend.
  - 2026-04-24: F-3 — endpoint `POST /internal/fiscal/callback` implementado com
    persistência em `fiscal_authority_callbacks` e espelho de histórico em
    `invoice.government_response.authority_callbacks`.
  - 2026-04-24: F-3 — endpoint `GET /admin/fiscal/danfe/{invoice_id}/pdf`
    adiciona DANFE PDF simplificado em `content_base64` (`pdf_stub_base64_v1`).
  - 2026-04-24: F-3 contingência slice — `route_issue_invoice` passa a respeitar
    `emission_mode` (`OFFLINE_SAT`, `CONTINGENCY_SVRS`) para BR, emitindo via
    `sefaz_contingency_service` com status `CONTINGENCY_PENDING_SYNC` e `sync_pending=true`.
  - 2026-04-24: F-3 contingência sync — novo `invoice_resync_service` (seleção de
    invoices com `government_response.sync_pending=true`, claim atômico com lock e
    reprocesso via `route_issue_invoice_reconnect`), integrado como loop adicional
    no `invoice_issue_worker`; sincronização bem-sucedida marca `sync_pending=false`
    (`sync_state=SYNCED`) e falha marca `sync_state=FAILED_RETRY` com reattempt.
  - 2026-04-24: F-3 XML/A1 scaffold — `nfce_xml_builder_v40` adicionado com
    `schema_version=4.00`, `xml_preview` NFC-e mínimo e bloco `signature` com anchors
    para assinatura A1 (`A1_STUB_PENDING`); `sefaz_sp_service` passa a preencher
    `xml_content` NFC-e com essa estrutura para `fiscal_doc_subtype=NFC_E_65`.
  - 2026-04-24: F-3 UX correções operacionais — PDF DANFE stub passa a gerar binário
    PDF válido (xref/trailer), e `order_pickup_service` adiciona fallback automático
    para `POST /internal/invoices/generate/{order_id}` quando `by-order` retorna 404
    nos fluxos de reenvio e download de invoice.
  - 2026-04-24: F-3 ops suporte — página `ops/fiscal/providers` recebe ação
    "Gerar invoice por order_id" (chama `POST /admin/fiscal/force-issue/{order_id}`);
    compose do `order_pickup_service` define `BILLING_FISCAL_SERVICE_URL` para habilitar
    proxy público de invoice (resend/pdf).
  - 2026-04-24: F-3 suporte manual no detalhe público — novo endpoint
    `POST /public/orders/{order_id}/invoice-generate` (role-gated: `admin_operacao`,
    `suporte`, `auditoria`) e botão "Gerar invoice agora" na página de detalhe;
    `force-issue`/`generate` no billing passam a aceitar `allow_missing_paid_event=true`
    para cenários de pagamento sem `order.paid` em `domain_events`.
  - 2026-04-24: F-3 UX invoice/manual fallback — detalhe público exibe badge
    "Invoice gerada manualmente (fallback sem domain_event)" e deixa de abrir links
    de comprovante legado para códigos de invoice vindos do billing quando
    `receipt_lookup_supported=false` (evita 404 em `/public/fiscal/by-code/*`).
  - 2026-04-24: F-3 UX suporte — detalhe público passa a exibir badge
    "Fonte: billing fiscal" e `invoice_id` explícito ao lado do código para
    facilitar triagem operacional.
  - 2026-04-24: F-3 UX suporte — detalhe público adiciona botão
    "Copiar invoice_id" para agilizar atendimento operacional.
  - 2026-04-24: F-3 UX padrão global — detalhe público passa a exibir
    "Estado fiscal" explícito (`PENDENTE`, `GERADO`, `GERADO_MANUALMENTE`,
    `SEM_DESTINATARIO_EMAIL`) com descrição operacional no card de invoice.
  - 2026-04-24: F-3 UX/CX suporte — estado fiscal ganha badge com ícone/cor
    para leitura instantânea (`GERADO` verde, `GERADO_MANUALMENTE` âmbar,
    `SEM_DESTINATARIO_EMAIL` azul, `PENDENTE` cinza), mantendo descrição textual.
  - 2026-04-24: F-3 onboarding UX — card de invoice no detalhe público recebe
    mini legenda de estados fiscais com significado operacional para novos operadores.
  - 2026-04-24: F-3 UX avançado — legenda de estados fiscais passa a ser colapsável
    ("Mostrar legenda" / "Ocultar legenda"), iniciando fechada para reduzir ruído.
  - 2026-04-24: Correção de consistência pós-retirada — `pickup_expiration_handler`
    passa a tratar `DISPENSED` e `EXPIRED*` como terminais na expiração tardia para
    evitar regressão de pedido já retirado para `EXPIRED_CREDIT_50`.


-------------------------------------------------------------------------------
SPRINT I-1 — Orders integration — da pickup ao modelo completo (Invoice)
-------------------------------------------------------------------------------
Duração referência: 2 semanas  |  Esforço: 13 pts
Objetivo: resiliência além do order_pickup_service; fallback orders DB;
          items_json multi-linha; locker_id resolvido; view reconciliação.

  User stories
  [x] INTEGRATION: fallback fiscal-context → invoice-source (ORM) → /status?full_fiscal=1
        em 404 ou timeout/5xx (billing_fiscal_service)
  [x] SERVICE: order_items no contrato v2 → items_json (F-1; reforçado no fallback status)
  [x] SERVICE: locker_id resolvido em build_fiscal_context (allocation/pickup → lockers)
  [x] SCHEMA: VIEW invoice_order_view no Postgres do billing (snapshot + items_json; sem cross-DB)
  [x] EVENT: order.paid v2 — totem_id + order_item_count no payload (outbox)

  Registro de evolução
  - 2026-04-23: I-1 — pickup: GET /internal/orders/{id}/invoice-source; /status?full_fiscal=1;
    billing: cadeia get_order_snapshot_for_invoice; startup VIEW invoice_order_view;
    order.paid com totem_id e order_item_count. MV cross-db não aplicável (invoices ≠ orders DB).


-------------------------------------------------------------------------------
SPRINT I-2 — Cancellation flow & document delivery (Invoice)
-------------------------------------------------------------------------------
Duração referência: 2 semanas  |  Esforço: 21 pts
Objetivo: política cancelamento SEFAZ (<30 min / 30min–24h CC / >24h NF comp.);
          e-mail DANFE; payload térmico; reissue com chave anterior.

  User stories
  [x] SERVICE: cancellation policy engine (BR <30m / 30m–24h / >24h) — cancellation_policy_service
  [x] SERVICE: saga CANCELLING → stub SEFAZ/AT/AEAT → CANCELLED (invoice_issue_worker)
  [x] DELIVERY: e-mail DANFE — invoice_delivery_log (QUEUED; conclusão SENT/SENT_STUB via worker F-3)
        ou SKIPPED_NO_RECIPIENT; gatilhos: pós-emissão (ISSUED), pós-cancelamento void, pós-CC-e stub
  [x] API: GET /internal/fiscal/danfe/{invoice_id}/thermal — JSON impressora (stub)
  [x] API: POST /internal/invoices/{id}/cce-request — fila CORRECTION_REQUESTED → worker CC-e stub (BR/PT/ES)
  [x] API: fluxo reissue — POST /internal/invoices/{id}/reissue preserva `previous_receipt_code`

  SCHEMA: novos InvoiceStatus — CANCELLING, CORRECTION_REQUESTED, COMPLEMENTARY_ISSUED +
           invoice_delivery_log (I-2 parcial)

  Registro de evolução
  - 2026-04-23: I-2 parcial — enum + invoice_delivery_log; request_invoice_cancel;
    POST /internal/invoices/{id}/cancel-request; worker processa CANCELLING; domain order.refunded;
    GET /internal/fiscal/danfe/{id}/thermal; estados CC/complemento sem worker SEFAZ real.
  - 2026-04-23: I-2 — e-mail (invoice_email_service + delivery log); worker CC-e stub
    (CORRECTION_REQUESTED→ISSUED, cce_events); sefaz_sp_cc_e_stub / at_pt / aeat; POST /cce-request.
  - 2026-04-23: I-2 + F-3 — e-mail enfileirado em `invoice_email_outbox`; worker emite SENT_STUB ou SENT (SMTP).
  - 2026-04-24: I-2 — reissue passa a derivar/salvar `previous_receipt_code` (protocolo/access_key/número)
    em `payload_json`, e retorna no `InvoiceResponse`.
  - 2026-04-24: I-2 UX/ops — reenvio de invoice por e-mail disponível no detalhe do pedido
    (`POST /public/orders/{order_id}/invoice-resend-email`), com backend billing aplicando
    cooldown/auditoria em `invoice_delivery_log`.


-------------------------------------------------------------------------------
SPRINT I-3 — Reconciliation, monitoring & fiscal reporting (Invoice)
-------------------------------------------------------------------------------
Duração referência: 2 semanas  |  Esforço: 13 pts
Objetivo: 1 invoice ISSUED por order.paid; dead letters; SPED BR; SAF-T PT.

  User stories
  [x] WORKER: reconciliação — PAID sem invoice / ISSUED sem PAID — cron alerts
  [x] API: GET /admin/fiscal/gaps?date= + POST /admin/fiscal/force-issue/{order_id}
  [x] EXPORT: job mensal SPED/EFD (BR)
  [x] EXPORT: job mensal SAF-T (PT, Portaria 302/2016)
  [x] MONITOR: /admin/fiscal/dead-letters + alertas threshold

  SCHEMA: fiscal_reconciliation_gaps

  Registro de evolução
  - 2026-04-24: I-3 parcial — tabela `fiscal_reconciliation_gaps`, serviço
    `fiscal_reconciliation_service` (scan + resolve), worker dedicado
    `invoice_reconciliation_worker`, endpoint `GET /admin/fiscal/gaps`
    (`refresh=1`, `status`, `date`, `limit`).
  - 2026-04-24: I-3 — endpoint `POST /admin/fiscal/force-issue/{order_id}` força
    emissão via orquestrador e resolve gap `paid_without_invoice:{order_id}` quando aberto.
  - 2026-04-24: I-3 — monitor `GET /admin/fiscal/dead-letters` (invoice/email outbox + threshold),
    exports `GET /admin/fiscal/exports/sped-efd` e `GET /admin/fiscal/exports/saft-pt`
    (payload mensal stub v1 para operacionalizar jobs cron).
  - 2026-04-24: OPS audit urgente (order_pickup_service) — endpoint
    `GET /dev-admin/orders-status-audit` para detectar pedidos potencialmente
    afetados no histórico (ex.: `picked_up_at` preenchido com `status` expirado),
    incluindo sinais de inconsistência order/pickup para triagem rápida.
  - 2026-04-24: OPS audit histórico — endpoints complementares
    `GET /dev-admin/orders-status-audit/range?from=&to=&limit=&offset=`
    (paginado por intervalo) e
    `GET /dev-admin/orders-status-audit/export.csv?from=&to=&limit=`
    (export CSV para investigação longa).


================================================================================
Quadro rápido de status por sprint (atualizar semanalmente)
================================================================================
  Sprint   Nome curto                              Status    Notas
  ------   ------------------------------------   ------    -----
  O-1      Contrato Orders + eventos              [x]
  F-1      Schema unificado NFC-e 65              [x]
  F-2      Tax engine BR/PT                       [x]
  F-3      Integração provedores + DANFE          [~]       smtp+cc-e+adapters+http retry/fallback
  I-1      Integração Orders resiliente           [x]       VIEW + fallbacks HTTP
  I-2      Cancelamento + entrega documento       [x]       cancel+CC-e+danfe thermal+reissue receipt code
  I-3      Reconciliação + exports + monitor      [x]       worker+gaps+force-issue+exports+dead-letters

Riscos / decisões pendentes (lista viva)
----------------------------------------
  -
  -

Próxima revisão sugerida do documento: _______________  Responsável: ___________

================================================================================
Fim do template de acompanhamento — editar este arquivo conforme as entregas.
================================================================================
