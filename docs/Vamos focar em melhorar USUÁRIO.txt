Plano de melhoria do USUARIO (ELLAN LAB)

Objetivo
- Melhorar a experiencia da area do usuario com foco em creditos, checkout, seguranca da conta e verificacao de e-mail.

Diagnostico atual
- Existe a tabela `credits`, mas nao existe tela para o usuario visualizar historico e saldo de creditos.
- No checkout, a aplicacao ainda nao oferece controle claro para aplicar ou nao credito.
- Nao existe fluxo visivel de troca de senha na area do usuario.
- O fluxo de confirmacao de e-mail ainda nao foi implementado.
- Ja existe `user_wallets` com `currency`, mas a regra entre carteira e creditos precisa ficar explicita.
- A tabela `user_roles` existe, mas ainda sem uso funcional.

Regras de negocio de credito (proposta)
1) O usuario pode aplicar credito no checkout manualmente (usar ou nao usar).
2) Apenas 1 credito por pedido.
3) Ao aplicar, selecionar automaticamente o credito disponivel com vencimento mais proximo (`expires_at` asc).
4) Nao permitir aplicacao de credito vencido, revogado ou ja utilizado.
5) O valor aplicado nao pode ultrapassar o total do pedido.
6) Creditos e carteira devem respeitar a moeda (`currency`) do contexto do usuario/pedido.

Decisao de modelagem de moeda
- Opcao recomendada: adicionar `currency` em `credits` para garantir rastreabilidade e validacao direta no momento do pagamento.
- Regra minima: `credits.currency` deve ser compativel com `user_wallets.currency` e com a moeda do pedido.
- Se nao for possivel adicionar campo agora, validar moeda via carteira do usuario como regra temporaria.

Requisitos funcionais prioritarios

P0 - Meus Creditos (nova pagina do usuario)
- Exibir saldo total disponivel.
- Listar historico com status (`AVAILABLE`, `USED`, `REVOKED`, `EXPIRED`), valor, origem e validade.
- Destacar os que estao proximos do vencimento.

P0 - Checkout com credito
- Exibir "Aplicar credito" com valor que sera abatido.
- Permitir marcar/desmarcar uso do credito antes de confirmar pagamento.
- Mostrar claramente o novo total apos aplicacao.

P1 - Conta e seguranca
- Criar fluxo de troca de senha (area logada).
- Incluir validacoes basicas de seguranca (senha atual obrigatoria e politica minima de nova senha).

P1 - Verificacao de e-mail
- Enviar link de verificacao no cadastro.
- Bloquear acoes sensiveis enquanto `email_verified = false` (conforme regra de negocio definida).
- Nao incluir verificacao por telefone por enquanto (decisao de custo).

P2 - Roles e permissoes
- Definir primeiro caso de uso real para `user_roles` (ex.: admin de operacao, suporte, auditoria).
- Ativar somente apos fechar matriz de permissao e escopo.

Requisitos tecnicos (backend)
- Endpoint para listar creditos do usuario com filtros por status e ordenacao por vencimento.
- Endpoint/servico para simular e aplicar 1 credito no checkout.
- Validacoes transacionais para evitar uso duplicado de credito.
- Auditoria em `used_at`, `revoked_at` e `order_id`.

Requisitos tecnicos (frontend)
- Nova tela "Meus Creditos" na area logada.
- Componente de aplicacao de credito no checkout com feedback em tempo real.
- Pagina "Seguranca" com troca de senha.
- Tela/estado de "E-mail nao verificado" com acao de reenviar link.

Criterios de aceite (MVP)
- Usuario consegue visualizar seus creditos e entender saldo/validade.
- Usuario consegue optar por usar ou nao 1 credito no checkout.
- Sistema sempre escolhe automaticamente o credito elegivel com vencimento mais proximo.
- Nao ha aplicacao de credito invalido (vencido, usado, revogado ou moeda incompativel).
- Usuario consegue trocar senha.
- Usuario recebe e confirma e-mail por link.

Tabelas de referencia (estado atual)

`public.credits`
SELECT id,
       user_id,
       order_id,
       amount_cents,
       status,
       created_at,
       updated_at,
       expires_at,
       used_at,
       revoked_at,
       source_type,
       source_reason,
       notes
FROM public.credits;

`public.users`
SELECT id,
       full_name,
       email,
       phone,
       is_active,
       email_verified,
       phone_verified,
       created_at,
       updated_at,
       anonymized_at,
       locale,
       totp_enabled,
       totp_secret_ref,
       created_by,
       updated_by,
       deleted_at
FROM public.users;

`public.user_wallets`
SELECT id,
       user_id,
       balance_cents,
       currency,
       status,
       last_transaction_at,
       created_at,
       updated_at
FROM public.user_wallets;

`public.user_roles`
SELECT id,
       user_id,
       role,
       scope_type,
       scope_id,
       is_active,
       granted_at,
       revoked_at
FROM public.user_roles;

Checklist de implementacao por sprint

Sprint 1 (P0) - Creditos visiveis + uso no checkout
STATUS: FECHADA (carimbo 22/04/2026) — contratos reais abaixo; ver testes em `order_pickup_service/tests/`.

Backend
- [x] Contrato listagem: `GET /public/me/credits` (auth Bearer). Lista ordenada (disponiveis primeiro, `expires_at` asc); agregados em `summary` (saldo disponivel, contagens, `expiring_soon_count` em 7 dias). Filtro por status no frontend (API devolve todos).
- [x] Implementar listagem de creditos do usuario autenticado (`app/routers/public_me.py`).
- [x] Implementar agregacao de saldo disponivel (`AVAILABLE` e nao vencido) no mesmo endpoint.
- [x] Contrato preview checkout: `GET /public/me/credits/checkout-preview` (query: `amount_cents`, `use_credit`, `credit_id` opcional, `region` para moeda do pedido).
- [x] Simulacao de 1 credito elegivel (mais proximo de expirar) no preview e em `select_checkout_credit_candidate` (`app/services/credits_service.py`).
- [x] Aplicacao de credito no `POST /public/orders/` via `create_order_core` (`use_credit`, `credit_id`) — nao ha endpoint separado `POST /checkout/apply-credit` (aplicacao transacional no commit do pedido).
- [x] Lock `SELECT FOR UPDATE`, validacao de status/vencimento, `db.flush` pos-USAGE, restauracao em compensacao de falha (`restore_credit_after_failed_order_creation`).
- [x] Auditoria em `credits.used_at`, `status`, `notes`; `order_metadata.credit_application` no pedido (assign dict novo para persistir JSONB).
- [x] Compatibilidade de moeda: carteira (`user_wallets`) vs `OnlineRegion.get_currency(region)` no preview e no apply; sem carteira nao bloqueia.

Frontend
- [x] Pagina `PublicMyCreditsPage.jsx` + rota na area logada.
- [x] Saldo total disponivel (summary).
- [x] Lista com status, origem, valor, validade; filtro de status.
- [x] Destaque “proximos de vencer (7 dias)” via `expiring_soon_count` + `days_to_expiration` nos itens disponiveis.
- [x] Checkout: opcao usar credito + preview em tempo real (`PublicCheckoutPage.jsx`, `previewCheckoutCredit`).
- [x] Checkbox marcar/desmarcar; fluxo DEV de simulacao tambem envia `use_credit`/`credit_id`.
- [x] Resumo com total apos credito; detalhe do pedido com faixa + secao “Crédito no checkout” (`PublicOrderDetailPage.jsx`).
- [x] Motivos de inelegibilidade no preview (`currency_mismatch`, `no_eligible_credit`, etc.).

QA / Testes
- [x] Unitario ordenacao: `tests/test_credits_expires_at_ordering.py` (`select_checkout_credit_candidate` → menor `expires_at`).
- [x] Unitario invalidos: mesmo arquivo (expirado + USED ignorados).
- [x] Integracao servico checkout + restauracao + segunda aplicacao na mesma sessao: `tests/test_credits_checkout_concurrency.py`.
- [x] Concorrencia Postgres: `test_concurrent_apply_same_credit_only_one_applies_postgres` (requer `ORDER_PICKUP_TEST_DATABASE_URL` no CI).
- [x] E2E leve HTTP: `tests/test_e2e_light_public_me_credits.py` (TestClient: listar creditos + preview `use_credit` false/true = simula “ligar/desligar” antes de pagar). Criacao de pedido online continua teste manual ou CI com schema completo.

Definicao de pronto da Sprint 1
- [x] Usuario ve creditos e saldo com clareza.
- [x] Usuario escolhe usar ou nao usar credito no checkout.
- [x] Sistema aplica no maximo 1 credito por pedido sem inconsistencias (persistencia + detalhe do pedido alinhados).

Comando sugerido (testes automatizados desta sprint, no servico):
cd 01_source/order_pickup_service && pytest tests/test_credits_expires_at_ordering.py tests/test_e2e_light_public_me_credits.py tests/test_credits_checkout_concurrency.py -v
(Para concorrencia real em Postgres: exportar ORDER_PICKUP_TEST_DATABASE_URL antes do pytest.)

Sprint 2 (P1) - Seguranca da conta + verificacao de e-mail
STATUS: FECHADA (testes validados em 23/04/2026)

Backend
- [x] Implementar endpoint de troca de senha autenticada (`POST /public/auth/change-password`).
- [x] Exigir senha atual para alteracao.
- [x] Aplicar politica minima de senha (8+, maiuscula, minuscula e numero).
- [x] Implementar emissao de token de verificacao de e-mail (JWT purpose=`email_verification`).
- [x] Implementar endpoint de confirmacao por link (`GET /public/auth/email-verification/confirm`).
- [x] Implementar endpoint de reenvio de verificacao (`POST /public/auth/email-verification/resend`).
- [x] Definir acoes sensiveis com `email_verified = true` (checkout real e creditos em `/public/me/credits`).
- [x] Implementar recuperacao de senha em nivel producao (`POST /public/auth/forgot-password`) com resposta anti-enumeracao.
- [x] Implementar redefinicao de senha por token (`POST /public/auth/reset-password`) com validacao de `purpose=password_reset`, TTL curto e politica de senha.
- [x] Implementar suporte a "Lembrar-me" no login (`remember_me`) com TTL de sessao estendido no backend (30 dias).

Frontend
- [x] Criar pagina "Seguranca" com formulario de troca de senha (`/seguranca`).
- [x] Validar campos (senha atual, nova senha, confirmacao).
- [x] Criar estado/tela de e-mail nao verificado (pagina de seguranca + aviso no checkout).
- [x] Incluir acao de reenviar e-mail de verificacao.
- [x] Exibir status de verificacao no perfil do usuario (pagina de seguranca).
- [x] Tornar o link "Esqueceu a senha?" funcional, com pagina `/recuperar-senha` para solicitar link e concluir redefinicao.
- [x] Integrar checkbox "Lembrar-me" ao payload de login para habilitar sessao persistente.

QA / Testes
- [x] Teste unitario: regras de validacao de senha.
- [x] Teste de integracao: troca de senha com senha atual incorreta/correta.
- [x] Teste de integracao: token de verificacao expirado e valido.
- [x] Teste E2E: cadastro -> recebimento de link -> confirmacao de e-mail.
- [x] Testes de autenticacao atualizados com cenarios de remember_me (TTL) e reset de senha por token (`tests/test_auth_security_sprint2.py`).

Definicao de pronto da Sprint 2
- [x] Usuario consegue trocar senha com seguranca.
- [x] Usuario recebe e confirma e-mail por link.
- [x] Regras de bloqueio por e-mail nao verificado funcionam como esperado.

Sprint 3 (P2) - Roles e permissoes
STATUS: EM ANDAMENTO (bloco 1 implementado em 23/04/2026)

Registro de incidente (23/04/2026)
- Cenário reproduzido com usuario comum `m00.marcos@gmail.com`: ao usar "DEV — Criar pedido e simular pagamento", a simulacao retorna erro de autorizacao (`ROLE_REQUIRED` em `/dev-admin/simulate-online-payment`), porem o pedido ONLINE foi criado mesmo assim com status `PAYMENT_PENDING` e com slot marcado como `RESERVED`.
- Evidencia: pedido `3cf0bf89-edd2-4fc6-9dc0-ed4a933df031` criado com credito aplicado e bloqueio de slot, apesar da falha no passo de pagamento DEV.
- Comportamento esperado de produto: para usuario sem role operacional, o fluxo DEV nao deve criar pedido; a autorizacao precisa ser validada antes de qualquer efeito colateral (criacao de pedido, aplicacao de credito, reserva de slot).
- Contraste de controle: com `admin.operacao@ellanlab.com` o fluxo concluiu corretamente (pedido pago e retiravel), confirmando que a guarda de role esta ativa no endpoint DEV, mas tardia no fluxo atual do frontend/orquestracao.

Regra para usuarios comuns (decisao funcional)
- Usuario autenticado sem registro ativo em `user_roles` deve ser tratado como `usuario_comum` (papel implicito, sem permissao operacional).
- `usuario_comum` acessa apenas fluxos publicos da conta (catalogo, checkout, pedidos, creditos, seguranca, verificacao de e-mail).
- `usuario_comum` nao acessa endpoints de operacao (`/dev-admin/*` e `/dev-admin/base/*`).
- A autorizacao de role deve ocorrer antes da criacao de pedido em fluxos DEV (fail-fast), ou o backend deve compensar transacionalmente qualquer efeito colateral em caso de falha de role.

Matriz usuario -> roles -> endpoints permitidos/bloqueados (estado atual + regra alvo)
- Usuario: `admin.operacao@ellanlab.com` | Roles ativas: `admin_operacao (GLOBAL)` | Permitidos: `/dev-admin/*`, `/dev-admin/base/*`, `/public/*` autenticado | Bloqueados: nenhum do escopo atual.
- Usuario: `suporte@ellanlab.com` | Roles ativas: `suporte (GLOBAL)` | Permitidos: `/dev-admin/base/*`, `/public/*` autenticado | Bloqueados: `/dev-admin/*` (exceto base).
- Usuario: `auditoria@ellanlab.com` | Roles ativas: `auditoria (GLOBAL)` | Permitidos: `/dev-admin/*`, `/dev-admin/base/*`, `/public/*` autenticado | Bloqueados: nenhum do escopo atual.
- Usuario: `m00.marcos@gmail.com` | Roles ativas: nenhuma (=> `usuario_comum` implicito) | Permitidos: `/public/*` autenticado, incluindo `GET /public/auth/me/roles` (retorna lista vazia) | Bloqueados: `/dev-admin/*`, `/dev-admin/base/*`.

Backend
- [x] Mapear papeis iniciais (admin_operacao, suporte, auditoria) como primeiro caso de uso operacional.
- [x] Definir matriz inicial por role e escopo (consulta por `scope_type`/`scope_id` no serviço de roles).
- [x] Implementar guard de autorizacao reutilizavel (`require_user_roles`) e aplicar em endpoints operacionais (`/dev-admin`).
- [x] Implementar consulta de roles ativas por usuario (`GET /public/auth/me/roles`).
- [x] Formalizar regra de `usuario_comum` (papel implicito sem grant em `user_roles`) na documentacao de autorizacao e nos contratos de frontend/backend.
- [x] Publicar politica como fonte unica no OpenAPI (`app/main.py` via `AUTHORIZATION_POLICY_MD`).
- [x] Expor documentacao interna somente leitura (`GET /internal/docs/authorization`, protegido por token interno).
- [x] Expor endpoint publico somente leitura para consumo do frontend (`GET /public/auth/authorization-policy`).
- [x] Hotfix de compensacao no cancelamento publico (`POST /public/orders/{order_id}/cancel`): reconciliar pedido `CANCELLED` com tentativa de `locker_release`, marcacao de allocation como `RELEASED`, cancelamento de pickup ativo e restauracao de credito via `restore_credit_after_failed_order_creation`.
- [x] Ampliar reconciliacao no cancelamento publico para estados `EXPIRED` e `EXPIRED_CREDIT_50`.
- [x] Permitir reconciliacao segura de `PAID_PENDING_PICKUP` expirado (somente quando deadline efetivo ja passou), com fallback de deadline: `order.pickup_deadline_at` -> `pickup.expires_at`.
- [x] Corrigir expiração automática para reabrir crédito aplicado no checkout (jobs `expiry` + `pickup_expiration_handler` usando `restore_credit_after_failed_order_creation`).

Frontend
- [x] Condicionar exibicao de funcionalidades por permissao (links e rotas `/ops/*` baseados em roles).
- [x] Criar fallback de acesso negado (`/acesso-negado`).
- [x] Implementar cliente API para politica de autorizacao (`fetchPublicAuthorizationPolicy` em `services/authApi.js`).
- [x] Criar componente reutilizavel de renderizacao (`AuthorizationPolicyPanel.jsx`).
- [x] Criar pagina OPS dedicada para politica (`/ops/auth/policy`) com protecao por `OpsRoute`.
- [x] Endurecer botao DEV no checkout: exibir simulacao apenas para roles autorizadas (`admin_operacao`/`auditoria`) e tentar compensacao automatica (cancel) quando houver `ROLE_REQUIRED` no passo de simulacao.
- [x] Meus Créditos: notas com `order_id=<uuid>` agora viram links para o detalhe (`Ver pedido`).

QA / Testes
- [x] Teste unitario: avaliacao de permissao por role e escopo (`tests/test_user_roles_sprint3.py`).
- [x] Teste de integracao: endpoint de roles e consulta protegida por role no backend.
- [ ] Teste E2E: usuario com perfil comum nao acessa telas administrativas (pendente automacao E2E; fluxo validado manualmente na UI).
- [x] Regressao critica (hotfix backend/frontend): falha de autorizacao no passo DEV passa a acionar compensacao via cancelamento com reconciliacao de credito/slot no backend.
- [ ] Teste E2E negativo: usuario comum tentando "Criar pedido e simular pagamento" deve falhar sem persistir pedido e sem alterar estado da gaveta.
- [x] Teste de compensacao adicionado: `tests/test_public_order_cancel_compensation.py` (marcado para ambiente Postgres via `ORDER_PICKUP_TEST_DATABASE_URL`).
- [x] Validacao manual concluida: reconciliacao funcionou em pedidos reais com slot preso e crédito usado (incluindo casos `CANCELLED` e `PAID_PENDING_PICKUP` expirado sem `pickup_deadline_at` preenchido).

Definicao de pronto da Sprint 3
- [x] Primeiro caso de uso de `user_roles` ativo em producao.
- [x] Acessos sensiveis protegidos por matriz de permissao.
- [x] Fluxos DEV/operacionais com compensacao transacional em falha parcial: cancelamento publico reconcilia credito + slot e permite reparo de pedidos `CANCELLED`, `EXPIRED*` e `PAID_PENDING_PICKUP` expirado.

Checklist tecnico transversal (executar em todas as sprints)
- [ ] Padronizar codigos de erro e mensagens de API.
- [ ] Adicionar logs estruturados para eventos criticos.
- [ ] Instrumentar metricas (taxa de uso de credito, falha de verificacao, erro de permissao).
- [ ] Revisar migrations e estrategia de rollback.
- [ ] Atualizar documentacao de API e fluxos de produto.

Sprint 4 (P1/P2) - Confiabilidade operacional + UX de reconciliação (1-2 semanas)
STATUS: PLANEJADA

Objetivo da sprint
- Reduzir incidentes de inconsistência entre pedido/crédito/slot e melhorar rastreabilidade para usuário e operação.

Item 1 - Reconciliacao transacional unificada
- Escopo:
  - Consolidar regra de compensação para fluxos de falha em um serviço único (cancel, expiração, falha pós-criação).
  - Garantir idempotência da reconciliação por `order_id`.
- Criterios de aceite:
  - Repetir reconciliação no mesmo pedido não cria efeitos duplicados.
  - Pedido com crédito aplicado e slot preso termina com: crédito `AVAILABLE` + allocation `RELEASED`.
  - Resposta da API informa resultado da compensação por etapa (`credit_restored`, `slot_release_ok`).
- Validacao tecnica:
  - `cd 01_source/order_pickup_service && pytest tests/test_credits_checkout_concurrency.py tests/test_public_order_cancel_compensation.py -v`
- Validacao manual:
  - Criar cenário com falha de simulação/expiração, executar reconciliação 2x no mesmo `order_id` e confirmar estado final estável (sem duplicação).

Item 2 - Observabilidade mínima (logs + correlação)
- Escopo:
  - Padronizar logs estruturados com chaves: `order_id`, `allocation_id`, `credit_id`, `user_id`, `region`, `locker_id`, `correlation_id`.
  - Garantir logs de início/fim/erro para reconciliação e expiração.
- Criterios de aceite:
  - Cada fluxo crítico gera log de sucesso ou erro com os identificadores mínimos.
  - Erros de integração (runtime/lifecycle) ficam pesquisáveis por `order_id`.
- Validacao tecnica:
  - `docker compose logs order_pickup_service --tail=200 | rg "order_id|allocation_id|credit_id|correlation_id"`
- Validacao manual:
  - Executar um cancelamento com reconciliação e verificar no log as etapas e resultado final.

Item 3 - Padronização de erros de API
- Escopo:
  - Aplicar contrato único de erro em endpoints críticos (`type`, `message`, `retryable`, `action`, `correlation_id` quando aplicável).
  - Uniformizar mensagens de `CANCEL_NOT_ALLOWED`, `ROLE_REQUIRED` e falhas de integração.
- Criterios de aceite:
  - Endpoints críticos não retornam erro bruto de exceção para cliente final.
  - Frontend consegue exibir mensagem amigável sem parsing ad-hoc por endpoint.
- Validacao tecnica:
  - `cd 01_source/order_pickup_service && pytest -q`
  - `cd 01_source/frontend && npm run build`
- Validacao manual:
  - Forçar erros conhecidos (sem role, pedido inválido, cancel fora de regra) e confirmar payload consistente.

Item 4 - UX do pedido e créditos (rastreabilidade)
- Escopo:
  - Exibir linha de eventos básicos no detalhe do pedido (criado, pago, expirado/cancelado, crédito aplicado/reaberto quando houver).
  - Manter navegação cruzada pedido <-> crédito com links consistentes.
- Criterios de aceite:
  - Usuário entende por que o pedido expirou/cancelou e o que ocorreu com o crédito.
  - Link de crédito para pedido (e vice-versa quando aplicável) funcionando.
- Validacao tecnica:
  - `cd 01_source/frontend && npm run build`
- Validacao manual:
  - Abrir um pedido com crédito aplicado e outro reaberto por compensação; validar timeline e links na UI.

Item 5 - E2E negativo de autorização OPS
- Escopo:
  - Automatizar teste E2E de usuário comum sem role tentando acessar rotas `/ops/*`.
  - Cobrir também tentativa de fluxo DEV sem role (sem persistir pedido/slot/credito indevido).
- Criterios de aceite:
  - Usuário sem role recebe bloqueio (`/acesso-negado`) em todas as rotas OPS testadas.
  - Tentativa de DEV sem role não deixa pedido persistido nem slot reservado.
- Validacao tecnica:
  - `cd 01_source/frontend && npm run build`
  - `cd 01_source/order_pickup_service && pytest tests/test_user_roles_sprint3.py tests/test_public_order_cancel_compensation.py -v`
- Validacao manual:
  - Logar com usuário comum, tentar `/ops/sp/kiosk` e ação DEV; confirmar bloqueio e ausência de efeitos colaterais.

